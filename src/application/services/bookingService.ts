export class BookingService {
  async bookTour(userId: string, tourId: string, participants: number) {
    // Kiểm tra điều kiện và tạo booking
  }

  async cancelBooking(bookingId: string) {
    // Hủy đơn đặt tour
  }
}
