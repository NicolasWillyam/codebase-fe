export class PromotionService {
  async applyDiscount(tourId: string, code: string) {
    // Áp dụng mã giảm giá
  }

  async getActivePromotions() {
    // Trả về danh sách khuyến mãi đang chạy
  }
}
