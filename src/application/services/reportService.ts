export class ReportService {
  async generateTourReport() {
    // Trả về thống kê tổng hợp
  }

  async getTopTours(limit: number) {
    // Trả về top tour được đánh giá cao nhất hoặc nhiều lượt đặt nhất
  }
}
